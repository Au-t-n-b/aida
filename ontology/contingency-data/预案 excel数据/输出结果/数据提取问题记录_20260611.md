# 交付预案页面数据提取与问题记录

提取来源：`http://127.0.0.1:5174/proposal`

提取时间：2026-06-11

结构化抽取文件：`proposal_page_data_20260611.json`

说明：本次以 live 页面和 Vite 服务模块为准。当前 5174 页面服务响应中的组件路径显示为 `D:/Code/zhubo/aida/frontend/...`，与当前工作目录 `D:/Code/aida` 的源码存在差异，所以没有使用当前仓库源码作为唯一口径。

## 已写入的表

| 文件 | 写入行数 | 备注 |
|---|---:|---|
| `共平面类型表.xlsx` | 4 | 四类共平面均为选中，版本号取页面版本 |
| `智算部件配置信息表.xlsx` | 3 | 来自页面部件配置 |
| `服务内容表.xlsx` | 19 | 来自页面服务树，单位列页面未给出，保留空 |
| `维护策略表.xlsx` | 5 | 数量列页面未给出，保留空 |
| `网管服务器.xlsx` | 3 | 来自页面 5.2 网管服务器配置 |
| `网络平面配置表.xlsx` | 5 | 来自页面 5.1 网络平面配置 |
| `设备信息表.xlsx` | 6 | 页面未展示 GA/EOM/EOS 计划/实际日期，相关列保留空 |
| `软件配置信息表.xlsx` | 7 | 来自页面软件配置 |
| `预集成预验证需求信息表.xlsx` | 3 | 来自页面集成验证需求 |
| `测试用例.xlsx` | 54 | 来自页面测试用例模块，默认全部纳入验收 |

## 已有数据但需复核的表

| 文件 | 当前行数 | 页面抽取行数 | 问题 |
|---|---:|---:|---|
| `机房机柜信息表.xlsx` | 57 | 9 | 当前表明显多于页面 PoD 行数，且存在锁文件，未覆盖 |
| `项目责任矩阵.xlsx` | 65 | 62 | 当前表比页面模块多 3 行，需要确认是否为人工补充或旧版本残留 |
| `服务交付界面表.xlsx` | 13 | 13 | 已有数据，页面当前交付界面前两行显示为“华为”，与旧 `SERVICE_CONFIG` 的“伙伴”口径不同 |
| `维保SLA表.xlsx` | 13 | 13 | 已有数据，与页面 SLA 分组行数一致 |
| `验收策略.xlsx` | 7 | 7 | 有锁文件，未改动 |
| `项目背景信息表.xlsx` | 1 | 1 | 已有数据，与页面项目背景一致 |

## 未写入或缺少的数据

| 项 | 状态 | 处理 |
|---|---|---|
| `集群设备清单表.xlsx` | 文件为空，但存在 `~$集群设备清单表.xlsx` 锁文件 | 未写入；JSON 中已抽取 4 行集群设备数据 |
| `10. 计划` | 页面有 3 行活动，但当前输出目录没有计划表模板 | 已在 JSON 中抽取，未新增 Excel |
| 合同信息 | 页面模块数据有 2 条合同，但 live 正文未渲染合同表，当前目录无合同表 | 记录为口径差异 |
| 风险列表 | 页面模块数据有 5 条风险，但 live 正文未渲染风险表，当前目录无风险表 | 记录为口径差异 |
| 假设表 | 页面模块数据有 4 条假设，但 live 正文未渲染假设表，当前目录无假设表 | 记录为口径差异 |
| 项目基础信息表 / 预案版本信息表 | 当前 live 正文只有顶部元数据和修改记录，当前目录无对应表 | 如按历史 22 表口径，需要补模板或恢复历史表 |

## 锁文件

当前目录存在 3 个 Excel 临时锁文件：

| 锁文件 | 对应表 | 影响 |
|---|---|---|
| `~$机房机柜信息表.xlsx` | `机房机柜信息表.xlsx` | 未覆盖 57 行旧数据 |
| `~$集群设备清单表.xlsx` | `集群设备清单表.xlsx` | 未写入 4 行页面集群数据 |
| `~$验收策略.xlsx` | `验收策略.xlsx` | 表已有 7 行，未改动 |

## 建议下一步

1. 关闭上述 Excel 文件后，先补写 `集群设备清单表.xlsx`。
2. 决定 `机房机柜信息表.xlsx` 是否以页面 9 个 PoD 行为准，若是则需要覆盖当前 57 行。
3. 明确输出口径：只按 live 正文 1-12 章，还是沿用历史 22 表口径。若沿用 22 表，需要恢复合同、风险、假设、项目基础、预案版本等表。
