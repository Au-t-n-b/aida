# ── aida/agent 后端地址（LangGraph skill + SOG 孪生 API）──────────────────────
# 本地开发（默认）：
VITE_AGENT_BASE=http://127.0.0.1:7401
# 内网生产（ServerB）：
# VITE_AGENT_BASE=http://10.143.2.198:7401

# ── 系统设计模块 skill_id（前端 design 模块路由）──────────────────────────────
# system_design — 完整 A3 交付流（线性 DAG + 内嵌 a3_engine，默认）
# xtsj          — dispatch 命令 PoC（legacy）
# VITE_DESIGN_SKILL=xtsj

# ── ClawManager 后端地址（鉴权 + 项目列表 + 聊天票据）────────────────────────
# 本地开发：连本机 Manager（代理远端数据中心 231:8000）
VITE_CLAWMANAGER_BASE=http://127.0.0.1:8081
# 内网生产（ServerA · 旧版可能无 /api/v1/projects/my）：
# VITE_CLAWMANAGER_BASE=http://10.143.2.197:8000
