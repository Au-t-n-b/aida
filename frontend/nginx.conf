server {
    listen       5401;
    server_name  _;

    # 前端静态文件
    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 512m;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 256;

    # ── Manager API 代理（鉴权 / 项目列表 / Chat）──
    location /api/v1/auth {
        proxy_pass         http://manager:8081;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }
    # proposal / delivery-plan 由 Agent 提供，需先于 /api/v1/projects 命中。
    location ~ ^/api/v1/projects/[^/]+/proposal(?:/.*)?$ {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }
    location ~ ^/api/v1/projects/[^/]+/delivery-plan(?:\.xlsx|/.*)?$ {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }
    location /api/v1/projects {
        proxy_pass         http://manager:8081;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }
    location /api/v1/chat {
        proxy_pass         http://manager:8081;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }

    # ── Agent API 代理（skill / 交付计划 / SSE 流）──
    location /agent {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        # SSE 流式支持
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        proxy_buffering    off;
        proxy_cache        off;
        chunked_transfer_encoding off;
        proxy_read_timeout 3600s;
    }
    location /api/sog {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 3600s;
    }
    location /data/sog-assets {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 3600s;
    }
    location /api/v1 {
        proxy_pass         http://agent:7401;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
    }

    # ── SPA 回退 ──
    location / {
        try_files $uri $uri/ /index.html;
    }
}
