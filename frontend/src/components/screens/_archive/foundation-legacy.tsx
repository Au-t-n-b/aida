'use client';

/** 归档：原 /foundation 轻量入口，已由 /twin 完整 3D 孪生替代 */
export default function FoundationScreen() {
  return (
    <div style={{ padding: 24, color: 'var(--c-text-muted)', fontSize: 13 }}>
      算力底座孪生已迁移至 <a href="/twin">/twin</a>（3D 孪生世界）。
    </div>
  );
}
