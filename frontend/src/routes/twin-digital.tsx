import { TwinDigitalPanel } from '@/components/twin/twin-world';

export default function TwinDigitalPage() {
  return (
    <div style={{ flex: 1, display: 'flex', flexDirection: 'column', minHeight: 0 }}>
      <TwinDigitalPanel />
    </div>
  );
}
