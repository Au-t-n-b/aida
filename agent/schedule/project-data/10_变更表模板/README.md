# 变更表模板

这是 T-013 新增的系统生成资产目录，区别于 `02_项目数据/01~10` 的业务源表。

- `变更表模板.xlsx`：固定三 sheet 模板，由 openpyxl 生成，无嵌入图片。
- `表头.md`：模板表头与字段含义说明。

重生成方式：在仓根用 openpyxl 创建三张 sheet，表头保持与 `表头.md` 完全一致。该模板只承载上传入口的确定性变更解析，不作为业务源表维护。
