# 排期后端依赖（Web 服务子集，对齐 jintao-aida《docs/08_后端技术栈.md》版本下限，2026-06-10）
# 安装方法见同目录《环境安装.md》（内网用默认华为镜像；外网见 README 的离线 wheel 方法）
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
python-multipart>=0.0.12
openpyxl>=3.1
httpx>=0.27
pytest>=8.0
# SQLite 版本存储使用 Python 标准库 sqlite3，无需额外依赖。
